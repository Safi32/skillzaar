specVersion: "v1"
serviceId: "skillzaar"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "skillzaar-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
