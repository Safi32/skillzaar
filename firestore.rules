rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Testing rules - allow all read/write operations for development
      // This is for testing purposes only
      allow read, write: if true;
    }
  }
}
